apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote-service
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote-service
  template:
    metadata:
      labels:
        app: vote-service
    spec:
      containers:
        - name: vote-service
          image: <ACR_LOGINSERVER>/vote-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
---
apiVersion: v1
kind: Service
metadata:
  name: vote-service
  namespace: app
spec:
  selector:
    app: vote-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
